# change-data-pipeline
Description: Build a CDC pipeline that streams DB changes into an event log, supports consumers, replay, and schema evolution. Provide a demo consumer that builds projections. Shows: data engineering + distributed event systems. Stack: Postgres logical decoding (or Debezium) + Kafka/Redpanda or simplified event log.
